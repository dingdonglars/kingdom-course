# Quiz — B1.3

## 1. What's SSMS?

a. A database
b. SQL Server Management Studio — the official Windows GUI for SQL Server
c. A backup tool
d. Required for production

## 2. Cross-platform alternative to SSMS?

a. None
b. Azure Data Studio (macOS/Linux/Windows; same job, slightly different UI)
c. SQLite Browser
d. Required to use Windows

## 3. What's the value of the Estimated Execution Plan?

a. Visualises how the DB plans to execute a query — index scans/seeks, joins, etc. Read it when a query is slow.
b. Required for queries
c. Performance benchmarking
d. Backup tool

## 4. The lesson says "tools beat hammers." What's the meaning?

a. Professional tools (SSMS) make work that's *possible* without them faster. Knowing the tool compounds.
b. SSMS is the only way
c. Hammers are bad
d. Tradition

## 5. Right-click DB → Generate Scripts is for...

a. Performance
b. Exporting schema (and optionally data) as a `.sql` file — useful for backups, sharing, version control
c. Hashing
d. Required at install