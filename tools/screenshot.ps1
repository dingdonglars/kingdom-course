# screenshot.ps1 — capture browser screenshots from kingdom-reference at a tag
#
# STATUS: skeleton — filled in during p006 (Block 6 authoring).
# See tools/README.md for target usage.

[CmdletBinding()]
param(
  [Parameter(Mandatory = $true)] [string] $ReferenceTag,
  [Parameter(Mandatory = $true)] [string] $OutputDir
)

Write-Host "screenshot.ps1 is a stub. Implementation lands in p006."
Write-Host "Args received: -ReferenceTag $ReferenceTag -OutputDir $OutputDir"
exit 1
</content>
</invoke>