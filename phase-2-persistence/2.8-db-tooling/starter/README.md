# Module 2.8 starter — DB tooling

No code changes. The starter ships:

- `tools/sqlite-tour.md` — one-page cheatsheet (DB Browser, `sqlite3` CLI, VS Code SQLTools, `dotnet ef migrations script`)
- `tools/sample-queries.sql` — 5 ready-to-copy queries against the kingdoms DB

Drop these in your repo's `tools/` folder. Build/test status unchanged from M2.7 (63 passing).