# <YOUR-REPO-NAME>

> Your repo. You named it. This README is the starter — replace it with your own when you have something to say.

## What this is

This is your Kingdom course repo. By month 12 it will hold a console game, a persisted version of the same game, a public web API, and a browser-playable version of the Kingdom. By the bonus phase it will also hold a Roblox port.

You're going to build all of it yourself. Lars mentors throughout. Claude joins as your AI assistant from M3.9 onwards (the AI Unlock — about halfway through the year; rules in `ai-tools.md`). You ship.

## What's in here on Day 1

- `STANDARDS.md` — the code, naming, and PR conventions you'll follow. Synced from the course repo; do not edit.
- `CLAUDE.md` — the AI's machine-side rules. Doesn't do anything until Claude arrives at M3.9 (auto-loaded by Claude Code from then on). Read `ai-tools.md` for the plain-English version + timing.
- `.claude/commands/` — slash commands that activate when Claude arrives at M3.9 (`/explain-this-concept`, `/code-review`, `/stuck-on-error`, `/walk-through-code`, `/lesson-review`, `/milestone-review`). Type `/` in Claude Code (once you have it) to list them.
- `.editorconfig`, `.gitignore` — the boring foundations.
- `.github/PULL_REQUEST_TEMPLATE.md` — what your PR bodies look like.
- `journal/wins.md` — write here every time you finish a milestone.
- `journal/reflections/` — write here when you want to think out loud.

## What's NOT in here on Day 1

Code. You'll add that.

## How to start

Open the course repo. Open `phase-0-spark/0.0-setup/lesson.md`. Begin.

## Help

Read `MENTOR-PROTOCOL.md` at the root of the course repo. Post in Slack `#help` when stuck (after the 20-minute rule). From M3.9 onwards, Claude is also available — bucket rules in `ai-tools.md`.

Welcome to the Kingdom.
