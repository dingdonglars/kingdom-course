# starter/

The Inventory Tool v1 from Module 0.7. Happy-path only — Module 0.8 adds error handling.

```powershell
dotnet run
```

Commands: `add <item>`, `remove <item>`, `find <item>`, `list`, `save`, `load`, `help`, `quit`.