# starter/

The Inventory Tool v2 from Module 0.8 (with try/catch + validation). Run normally:

```powershell
dotnet run
```

Or from VS Code press `F5` to debug — `.vscode/launch.json` is configured. Set breakpoints in the gutter to the left of any line.