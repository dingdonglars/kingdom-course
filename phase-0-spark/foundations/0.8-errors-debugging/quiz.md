# Quiz — Module 0.8

## 1. What does `try { ... } catch (Exception ex) { ... }` do?

a. Always runs the catch block
b. Runs the try block; if it throws, the catch block runs instead
c. Runs the catch block first, then the try block
d. Crashes if the try block throws

## 2. Why catch specific exception types (`IOException`) before catching the generic `Exception`?

a. Compiler requires it
b. Performance reasons
c. Catches are matched top-to-bottom; the first matching catch runs. Specific first means you can react differently to different problems.
d. There's no real reason

## 3. What's a breakpoint?

a. A line where the program crashes
b. A marked line where the debugger pauses execution so you can inspect state
c. The end of a method
d. A `try`/`catch` boundary

## 4. In the VS Code debugger, what does `F10` (Step Over) do?

a. Runs the current line and moves to the next line in the same method
b. Dives into the method called on the current line
c. Stops the debugger
d. Restarts the program

## 5. What's the call stack?

a. A pile of unread error messages
b. The chain of method calls that led to the current point of execution
c. A list of breakpoints
d. The history of pressed keys

---

When you're done, jot your answers and a sentence of reasoning in `journal/quiz-notes.md` — same layout as the entries that came before. Bring whichever you're least sure about to the next weekly sync.
