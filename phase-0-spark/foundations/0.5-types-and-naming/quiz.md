# Quiz — Module 0.5

## 1. What does `int gold = 5 / 2;` set `gold` to?

a. `2.5`
b. `2`
c. `3` (rounded)
d. An error — `5 / 2` isn't an `int`

## 2. What does `(int)3.99` give you?

a. `3` (truncated)
b. `4` (rounded)
c. An error — you can't cast a double to an int
d. `3.99` (cast does nothing)

## 3. Which of these compiles?

a. `int x = "5";`
b. `bool ready = "true";`
c. `string name = 42;`
d. `string? nickname = null;`

## 4. By convention, what should the name of a class look like?

a. `myClass` (camelCase)
b. `MyClass` (PascalCase)
c. `_myClass` (with underscore)
d. `MY_CLASS` (UPPER_SNAKE)

## 5. Why is `_camelCase` (with the underscore) used for private fields?

a. It's a Microsoft tradition with no real reason
b. To distinguish them from local variables and parameters at a glance
c. To make them harder to read
d. Required by the compiler