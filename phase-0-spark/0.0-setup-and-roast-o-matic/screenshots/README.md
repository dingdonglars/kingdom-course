# screenshots/ — Module 0.0

Screenshots Lars captures during a dry-run of Module 0.0. These belong here:

- `01-github-create-repo.png` — the GitHub "New repository" page with the form filled in.
- `02-vscode-terminal-hello-world.png` — VS Code's integrated terminal showing `Hello, World!` after the first `dotnet run`.
- `03-vscode-program-cs-roasts.png` — the modified `Program.cs` open in VS Code with the roast list visible.
- `04-github-after-push.png` — the GitHub repo page in the browser after `git push`, showing the commit.

**Capture conventions** (per spec §20.3):
- No learner-name in any visible UI text or page title.
- No GitHub avatar in the captured browser shell (use a fresh-looking signed-in state for the demo, or crop).
- No identifying personal context.

These are captured by hand by Lars on a dry-run machine, not by an automated tool. Replace this README with the actual PNGs once captured.
