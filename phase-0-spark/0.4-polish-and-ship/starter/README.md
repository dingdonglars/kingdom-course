# starter/

The polish toy from Module 0.4. Demonstrates ASCII art, console color, and reading/writing files. Run:

```powershell
dotnet run
```

`hero.txt` is created in the project folder after you enter your name. It's read back next run.
</content>
</invoke>