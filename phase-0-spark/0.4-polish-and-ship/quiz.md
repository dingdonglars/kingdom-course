# Quiz — Module 0.4

## 1. What does `Console.ForegroundColor = ConsoleColor.Yellow;` do?

a. Repaints the entire console window yellow
b. Sets the color of all *text written from now on* to yellow until you change it
c. Highlights the cursor in yellow
d. Throws an exception unless your terminal supports yellow

## 2. What does `Console.ResetColor()` do?

a. Resets the console to factory settings
b. Clears the console
c. Restores the foreground/background colors to their defaults
d. Resets the cursor position

## 3. What does `File.WriteAllText("hero.txt", name)` do?

a. Reads the contents of `hero.txt` into the variable `name`
b. Writes whatever is in `name` to a file called `hero.txt`, creating it if it doesn't exist (and overwriting it if it does)
c. Appends `name` to the end of `hero.txt`
d. Throws an error unless `hero.txt` already exists

## 4. Where does `hero.txt` end up?

a. In your Windows Documents folder, always
b. In the current working directory of the program (usually the project folder when you `dotnet run`)
c. On the desktop
d. In a hidden system location

## 5. The README anatomy has four sections. Which is NOT one of them?

a. What
b. How to run
c. Detailed API documentation
d. What's next

---

When you're done, jot your answers and a sentence of reasoning in `journal/quiz-notes.md` — same layout as the entries that came before. Bring whichever you're least sure about to the next weekly sync.
