# Quiz — Module 3.9

> Don't write your answers in this file — open `journal/quiz-notes.md` and write them there.
>
> Travou no inglês? Abra o `quiz.pt.md` — é este mesmo quiz em português. Tente em inglês primeiro.

## 1. The AI Unlock takes effect at...

- **a.** The end of Phase 0 (Spark Week + Foundations)
- **b.** The end of Phase 3 (Web API) — after M4. From then on, AI may help with implementation.
- **c.** The very start of the curriculum, before any modules
- **d.** Any time the learner decides to flip the flag

## 2. The post-unlock hard rule is...

- **a.** AI must always write the code so the learner can read it
- **b.** The learner must be able to explain every line of AI-generated code before merging — explanation is the merge rule
- **c.** AI may not be used for any implementation under any circumstance
- **d.** There's no specific rule; it's a soft guideline

## 3. The mode flag flips by editing...

- **a.** A row in a database table somewhere
- **b.** A single line (`Current mode: pre-unlock` → `post-unlock`) in `CLAUDE.md` — in workshop, course, and starter-template
- **c.** A C# constant in `Program.cs` and recompiling
- **d.** A configuration UI in the IDE

## 4. The post-unlock PR template adds an *AI assistance* section. What's in it?

- **a.** Just the name of the AI assistant used, nothing more
- **b.** Bot used + which lines you wrote vs which the AI wrote + anything you're unsure about
- **c.** A list of bugs the AI introduced
- **d.** Optional commentary that's never actually read

## 5. Why does the lesson call AI *a power tool, not an autopilot*?

- **a.** Power tools require respect. The discipline you've built (engine vs outer layer, deterministic tests, explanation as merge rule) is what lets you use AI without losing yourself.
- **b.** They are exactly equivalent metaphors
- **c.** AI is dangerous in all cases and should be avoided
- **d.** It's a tradition with no real meaning behind it

---

When you're done, jot your answers and a sentence of reasoning in `journal/quiz-notes.md` — same layout as the entries that came before. Bring whichever you're least sure about to the next weekly sync.
