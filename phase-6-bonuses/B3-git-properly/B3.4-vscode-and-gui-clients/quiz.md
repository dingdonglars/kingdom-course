# Quiz — Bonus B3.4

> Don't write your answers in this file — open `journal/quiz-notes.md` and write them there.
>
> Travou no inglês? Abra o `quiz.pt.md` — é este mesmo quiz em português. Tente em inglês primeiro.

## 1. What's the killer feature of VS Code's Source Control panel for unsloppy commits?

- **a.** It auto-generates commit messages from your changes
- **b.** It stages whole files faster than the terminal does
- **c.** It lets you stage individual lines (hunks) inside a file
- **d.** It commits and pushes in a single click

## 2. What does GitLens add that the built-in Source Control panel doesn't?

- **a.** Stage and commit operations
- **b.** Inline blame and a visual commit graph view
- **c.** Multi-repo support
- **d.** Push and pull buttons

## 3. When is the CLI faster than the VS Code panel?

- **a.** For staging and committing routine changes
- **b.** For scripts, hidden commands like `reflog` or `cherry-pick`, and over SSH
- **c.** For reading a side-by-side diff
- **d.** For viewing the commit graph

## 4. Do you need a dedicated GUI client like Fork or GitKraken?

- **a.** Yes — they're the standard professional tooling
- **b.** Only on Mac; Windows users typically use the CLI
- **c.** No — most working developers use the CLI plus their editor's git panel
- **d.** Yes if you're past the AI Unlock; the GUI is required post-unlock

## 5. The closing rule of B3 about CLI vs GUI is:

- **a.** Pick one (CLI or GUI) and stick with it; mixing is confusing
- **b.** Lean toward whichever is faster for the move at hand; CLI and GUI are a pair
- **c.** Always use the CLI for production; GUIs are for learners
- **d.** Use the GUI for shared branches and the CLI for personal branches

---

When you're done, jot your answers and a sentence of reasoning in `journal/quiz-notes.md` — same layout as the entries that came before. Bring whichever you're least sure about to the next weekly sync.
