# starter/

Module 1.1 starter — the Kingdom in one project (`Kingdom.Console`), inside the solution. Run:

```powershell
dotnet run --project Kingdom.Console
```

Module 1.2 splits this into engine + shell.
