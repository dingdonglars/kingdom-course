# starter/

Module 1.0 starter — the empty Kingdom solution: one console project (`Kingdom.Console`) that prints *Hello*. This is where you should be at the end of 1.0. Run:

```powershell
dotnet run --project Kingdom.Console
```

Module 1.1 fills `Kingdom.Console` with the first classes.
