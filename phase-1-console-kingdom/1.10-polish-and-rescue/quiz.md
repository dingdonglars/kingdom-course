# Quiz — Module 1.10 (ritual-themed)

## 1. The four sections of a good README are...

a. Author / License / Changelog / Contact
b. What / How to run / What you learned / What's next
c. Install / Build / Test / Deploy
d. Whatever the template says

## 2. What does `git stash` do?

a. Permanently deletes your changes
b. Sets uncommitted changes aside on a stack — recover with `git stash pop`
c. Pushes to the remote
d. Creates a new branch

## 3. The "rescue rule" is...

a. Always force-push
b. Read state (`git status`, `git log --oneline -10`) before acting on it
c. Always `git reset --hard`
d. Use the GUI

## 4. Why is `git reset --hard` called *destructive*?

a. It throws an error
b. It permanently discards uncommitted changes (and possibly committed work, depending on what you reset to)
c. It deletes the repo
d. It's not destructive

## 5. Why post a screenshot to `#wins`?

a. Vanity
b. The before/after one-liner is what your future self reads in a year — proof you grew. The Discord post is the public commitment.
c. Required by the curriculum
d. To signal availability for code review