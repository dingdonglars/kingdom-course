# Module 5.2 starter — Luau basics

The starter file `roblox-kingdom/scripts/luau-basics.lua` is a playground covering each syntax point from the lesson. Paste it into a Script in `ServerScriptService` and run it.

After running, walk through the Output panel; each `print` corresponds to a section of the lesson.