# Module 4.1 starter — HTML + CSS

A new top-level folder `web/` with three files:

- `web/index.html` — semantic skeleton
- `web/styles.css` — minimal sane styles
- `web/kingdom.js` — empty (Module 4.2 fills it)

Open `web/index.html` directly in your browser (double-click). The static structure renders.