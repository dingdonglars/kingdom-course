# Quiz — Module 4.0

> Don't write your answers in this file — open `journal/quiz-notes.md` and write them there.

## 1. What's *context engineering*?

- **a.** Choosing what the AI sees before it answers, so its output fits your project
- **b.** A C# pattern for managing dependency injection scope
- **c.** A logging library used in modern web frameworks
- **d.** A built-in feature of every AI chat product

## 2. The four-step frame for AI implementation requests is...

- **a.** Goal · where it goes · conventions · what *not* to do
- **b.** Title · description · author · date
- **c.** Try · catch · finally · throw
- **d.** Subject · verb · object · time

## 3. What's an *invented API*?

- **a.** A new endpoint you've added to your project recently
- **b.** A failure mode where the AI confidently calls methods that don't exist in your codebase
- **c.** An undocumented method in a third-party library
- **d.** An OpenAPI specification extension you haven't seen yet

## 4. Why curate a *reference set* (STANDARDS, ARCHITECTURE, examples)?

- **a.** The AI starts from your project's conventions instead of generic tutorial assumptions; the quality of the answer is bounded by the quality of the context
- **b.** Required by the .NET SDK before AI features will work
- **c.** Reviewers expect to see one in every commit you make
- **d.** It improves AI response speed measurably

## 5. The post-Unlock rule says you must be able to explain every line you ship. Why?

- **a.** AI-assisted code you can't explain is code that will rot — when it breaks, you can't debug it; when it's reviewed, you can't defend it
- **b.** It's required by the C# compiler for AI-touched files
- **c.** Linters will flag any code that's missing an explanation
- **d.** GitHub now blocks PRs without a written explanation for each line

---

When you're done, jot your answers and a sentence of reasoning in `journal/quiz-notes.md` — same layout as the entries that came before. Bring whichever you're least sure about to the next weekly sync.
