# Module 4.0 starter — context engineering unlock

The bulk is reading + writing your `ARCHITECTURE.md`. The starter ships:

- `ARCHITECTURE.md.template` — the structure to fill in
- `.claude/commands/implementation-help.md` — slash command for post-Unlock code requests

After applying:
- `ARCHITECTURE.md` lives at your repo root, populated for *your* project
- `.claude/commands/implementation-help.md` is installed; type `/` in Claude Code to confirm it lists
- Re-read `ai-context/CLAUDE.md` (which is now in `post-unlock` mode after M4)
