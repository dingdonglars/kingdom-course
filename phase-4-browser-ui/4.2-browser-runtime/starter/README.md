# Module 4.2 starter — DOM + fetch

Delta from M4.1:

- **MODIFIED:** `web/kingdom.js` — fetch the API, render to DOM
- **MODIFIED:** `Kingdom.Api/Program.cs` — adds CORS so the browser may call

Open `web/index.html` in a browser (after running the API). DevTools open the whole time.